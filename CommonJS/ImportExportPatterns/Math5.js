// to export more than 2 variables instead of using variable const, module.exports is used 

module.exports.add = (a,b) => {
    return a+b;
}
module.exports.subtract = (a,b) => {
    return a - b;
}


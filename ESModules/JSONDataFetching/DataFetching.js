// JSON data and Watch mode

const data = require('./Data.json')

console.log(data);
console.log(data.name);

// for json file , always import it with the extension .json 

// watch mode - restarts the process, when there is a change in the imported code
// node --watch filename  ------ IMPORTANT 
